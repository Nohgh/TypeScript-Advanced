//기본 타입 예제
let id: number = 5;
let company: string = "Corp";
let isPublished: boolean = true;
let x: any = "string";

//배열
let ids: number[] = [12, 2, 3];
let arr: any[] = [1, "a", true];

//튜플
let person: [number, string, boolean] = [1, "bard", true];

//열거형
enum Direction {
  Up = 1,
  Down,
  Left,
  Right,
}
